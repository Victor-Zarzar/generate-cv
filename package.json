{
  "name": "generate-cv",
  "private": true,
  "type": "module",
  "scripts": {
    "setup": "bun install && bunx playwright install",
    "build": "bun scripts/render.mjs",
    "pdf": "bun scripts/export-pdf.mjs"
  },
  "dependencies": {
    "playwright": "^1.50.0"
  }
}
